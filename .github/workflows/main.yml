on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     
      - name: "Testing"
        run : pwd

      - name: "Pushing to Docker hub "
         run: echo Team